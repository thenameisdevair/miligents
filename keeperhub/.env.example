# KeeperHub API Configuration
KEEPERHUB_API_KEY=kh_your_api_key_here
KEEPERHUB_API_URL=https://app.keeperhub.com

# HTTP Server Configuration (optional - leave unset for stdio mode)
# Set PORT to enable HTTP/SSE transport mode
PORT=3000

# MCP API Key for authenticating incoming requests (required when PORT is set)
MCP_API_KEY=your_secure_api_key_here
